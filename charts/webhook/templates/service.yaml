---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    app: {{ .Values.name }}
  ports:
  - name: webhook
    port: 80
    targetPort: 4785
