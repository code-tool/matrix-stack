---
# Source: synapse/templates/envoy-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: synapse-client-reader-envoy
  labels:
    app: synapse
    component: synapse-client-reader-envoy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: synapse
      component: synapse-client-reader-envoy
  strategy: 
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: synapse
        component: synapse-client-reader-envoy
      annotations:
        checksum/config: 4e5e008b7f7d9a5463aff65090872424c71bfd2f62a244863c06c80082de0999
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: envoy
          image: envoyproxy/envoy:v1.36.6
          imagePullPolicy: IfNotPresent
          args:
            - -c
            - /config/envoy.yaml
          ports:
            - name: http
              containerPort: 10000
              protocol: TCP
            - name: http-admin
              containerPort: 9901
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /ready
              port: http-admin
          readinessProbe:
            httpGet:
              path: /ready
              port: http-admin
          resources:
            
            {}
          volumeMounts:
            - name: config
              mountPath: /config/envoy.yaml
              subPath: envoy.yaml
            - name: config
              mountPath: /usr/lib/synapse.lua
              subPath: synapse.lua
      volumes:
        - name: config
          configMap:
            name: synapse-client-reader-envoy