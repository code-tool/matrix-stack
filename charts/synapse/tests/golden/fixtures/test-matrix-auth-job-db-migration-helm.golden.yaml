---
# Source: synapse/templates/matrix-authentication-job-db-migration.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: matrix-authentication-db-migration
  annotations:
    helm.sh/hook: "pre-install,pre-upgrade"
    helm.sh/hook-weight: "-20"
spec:
  parallelism: 1
  completions: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: db-migration
          image: ghcr.io/element-hq/matrix-authentication-service:1.18.0
          imagePullPolicy: IfNotPresent
          env:
            - name: "MAS_CONFIG"
              value: "/etc/mas-config.yaml"
          command:
            - /usr/local/bin/mas-cli
            - database
            - migrate
          volumeMounts:
            - name: config
              mountPath: "/etc/mas-config.yaml"
              subPath: "mas-config.yaml"
              readOnly: true
      volumes:
        - name: config
          secret:
            secretName: matrix-authentication
            items:
             - key: mas-config.yaml
               path: mas-config.yaml