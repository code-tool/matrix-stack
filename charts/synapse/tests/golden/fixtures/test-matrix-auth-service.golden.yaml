---
# Source: synapse/templates/matrix-authentication-service.yaml
apiVersion: v1
kind: Service
metadata:
  name:  matrix-authentication
  labels:
    app: synapse
    component: matrix-authentication
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: synapse
    component: matrix-authentication