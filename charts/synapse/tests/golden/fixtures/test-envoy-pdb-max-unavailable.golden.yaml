---
# Source: synapse/templates/envoy-pdb.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: synapse-client-reader-envoy
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: synapse
      component: synapse-client-reader-envoy