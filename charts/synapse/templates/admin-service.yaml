---
apiVersion: v1
kind: Service
metadata:
  name: admin
  {{- with .Values.admin.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    app: admin
  ports:
  - name: admin
    port: 80
    targetPort: 8080
