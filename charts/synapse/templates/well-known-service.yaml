---
apiVersion: v1
kind: Service
metadata:
  name: well-known
  {{- with .Values.wellKnown.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    app: well-known
  ports:
  - name: http
    port: 80
    targetPort: 80
